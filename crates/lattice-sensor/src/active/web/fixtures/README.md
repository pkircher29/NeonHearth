This self-signed certificate and its public test private key are synthetic test
fixtures generated solely for the credential-free web-discovery test. They are
not trusted identities, production credentials, or material to deploy. Expiry
and name mismatch are intentional: discovery reports certificate trust as
unverified while still checking the TLS handshake signature.
